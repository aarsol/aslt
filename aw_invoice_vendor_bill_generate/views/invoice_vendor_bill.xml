<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="cron_automated_generate_vendor_bill" model="ir.cron">
        <field name="name">Generate Vendor Bill</field>
        <field name="model_id" ref="aw_invoice_vendor_bill_generate.model_account_move"/>
        <field name="state">code</field>
        <field name="interval_number">1</field>
        <field name="interval_type">months</field>
        <field name="numbercall">-1</field>
        <field name="code">model.generate_vendor_bill()</field>
        <field name="doall" eval="False" />
        <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1, hour=0, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')" />
    </record>

</odoo>
